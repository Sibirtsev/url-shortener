var page = require('webpage').create(),
    args = require('system').args;

var filePath = args[1];

page.viewportSize = {
    width: {{ width }},
    height: {{ height }}
};

page.open('{{ url }}', function () {
    setTimeout(function(){
        page.render(filePath);
        phantom.exit();
    }, {{ wait }});
});